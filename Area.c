#include <stdio.h>

int main(){
    int l,b;
    printf("Enter Length and Breadth of the rectangle: ");
    scanf("%d%d",&l,&b);
    printf("Area of the Reactangle is %d",l*b);
    return 0;
}