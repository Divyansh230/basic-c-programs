#include <stdio.h>

int main(){
    int l,b;
    printf("Enter length and breadth of the rectangle:");
    scanf("%d%d",&l,&b);

    printf("Perimeter of the rectangle is %d",2*(l+b));
    return 0;
}
